services:
  helloworld:
    build: .
    ports:
      - "1085:8080"
    restart: unless-stopped
    environment:
      JAVA_OPTS: "-Xms32m -Xmx192m -XX:+UseSerialGC"
    volumes:
      - ./data/guestbook:/data/guestbook
