services:
  helloworld:
    build: .
    ports:
      - "80:8080"
    restart: unless-stopped
    environment:
      JAVA_OPTS: "-Xms32m -Xmx192m -XX:+UseSerialGC"
    volumes:
      - guestbook_data:/data/guestbook

volumes:
  guestbook_data:
